import styled from "styled-components";

const Container = styled.div``

const Product = () => {
    return (
        <Container>
        <div>Product</div>
        </Container>
    );
}

export default Product;